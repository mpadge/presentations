A clone of https://github.com/hakimel/reveal.js, retaining only the pieces necessary for a static site with default behaviour, which are:

- The main `index.html` page
- The `dist/` folder as pre-compiled by `vite build`.

All other files and folders have been deleted.
